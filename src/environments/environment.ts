export const environment = {
  production: false,
  firebase: {
    // Copy your firebase config in here
    // You can find these settings in "Project settings" -> "Web apps" section -> "Config"
  }
};
